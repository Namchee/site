<button
  aria-label="Pronounce Namchee"
  class=":uno: rounded-full grid place-items-center bg-surface transition-colors hover:bg-pressurized hover:text-content p-1 speech__button focus:bg-pressurized"
>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width="24"
    height="24"
    viewBox="0 0 24 24"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    class=":uno: w-4 h-auto speech__icon transition-colors stroke-current fill-transparent"
    ><path
      d="M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z"
    ></path>
    <path d="M16 9a5 5 0 0 1 0 6"></path>
    <path
      d="M19.364 18.364a9 9 0 0 0 0-12.728"></path>
  </svg>
</button>

<script>
  if (
    typeof window.speechSynthesis !== "undefined" &&
    typeof window.SpeechSynthesisUtterance !== "undefined"
  ) {
    const utterance = new window.SpeechSynthesisUtterance("Nam ché");
    utterance.lang = "id-ID";
    utterance.pitch = 1.35;

    const button = document.querySelector(".speech__button");
    const icon = document.querySelector(".speech__icon");

    button.addEventListener("click", () => {
      window.speechSynthesis.speak(utterance);
    });

    utterance.addEventListener("start", () => {
      button.classList.toggle("bg-content/25!");
      icon.classList.toggle("fill-content/75!");
      icon.classList.toggle("stroke-content/75!");
    });

    utterance.addEventListener("end", () => {
      button.classList.toggle("bg-content/25!");
      icon.classList.toggle("fill-content/75!");
      icon.classList.toggle("stroke-content/75!");
    });
  }
</script>
